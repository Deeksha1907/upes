<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Upes"
        tools:targetApi="31">

        <activity
            android:name=".presentation.screens.authentication.signup.signup"
            android:exported="true"
            android:theme="@style/Theme.Upes">

        </activity>
        <activity
            android:name=".presentation.screens.authentication.login.Login"
            android:exported="true"
            android:theme="@style/Theme.Upes">


        </activity>
        <activity android:name=".presentation.screens.authentication.lang"
            android:exported="true"
            android:theme="@style/Theme.Upes">

        </activity>
        <activity android:name=".presentation.screens.authentication.otp"
            android:exported="true"
            android:theme="@style/Theme.Upes">

        </activity>
        <activity android:name=".presentation.screens.authentication.level_sel"
            android:exported="true"
            android:theme="@style/Theme.Upes"/>

        <activity android:name=".presentation.screens.authentication.prior_know"
            android:exported="true"
            android:theme="@style/Theme.Upes"/>

        <activity android:name=".presentation.screens.authentication.Assessment_page"
            android:exported="true"
            android:theme="@style/Theme.Upes">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>